server:
  port: 8000
spring:
  application:
    name: cloud-config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/PratikAzad/kt-session-microservices-configuration
          search-paths: client-1,client-2